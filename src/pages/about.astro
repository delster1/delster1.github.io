---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="About">
  <h1>about this site</h1>
  <p>
    Astro ships zero JS by default. You opt-in to interactivity by hydrating a component (“island”).
    Files in <code>src/pages</code> become routes automatically.
  </p>
  <ul>
    <li>Edit <code>BaseLayout.astro</code> to change global UI</li>
    <li>Add images to <code>public/</code> and reference them as <code>/filename</code></li>
  </ul>
</BaseLayout>

